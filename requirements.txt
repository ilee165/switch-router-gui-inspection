PyQt6>=6.6.0
bcrypt>=4.0.0
netmiko>=4.3.0
ntc-templates>=3.0.0
paramiko>=3.0.0

# Linux/Mac only — structured parsing via Genie; not available on Windows.
# On Windows the app falls back to raw Netmiko output automatically.
# pyats>=23.0
# genie>=23.0